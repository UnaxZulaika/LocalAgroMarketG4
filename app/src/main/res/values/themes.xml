<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LocalAgroMarket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LocalAgroMarket" parent="Base.Theme.LocalAgroMarket" />

    <style name="BottomNavigation">
        <item name="android:background">@color/green_dark</item>
        <item name="itemIconTint">@color/green_light2</item>
        <item name="itemTextColor">@color/green_light2</item>
        <item name="itemActiveIndicatorStyle">@color/green_dark</item>
    </style>
</resources>